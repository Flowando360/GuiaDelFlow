/**
 * PLACEHOLDER — todavía no se generó desde el esquema real.
 *
 * Una vez que `supabase/migrations/0001_flow_schema.sql` esté aplicada en
 * el proyecto remoto, reemplazar este archivo corriendo:
 *
 *   supabase gen types typescript --project-id zmpggzrmsuudxyjtobzy --schema public > src/types/database.types.ts
 *
 * (requiere `supabase link` con un token que tenga acceso a ese proyecto).
 * Mientras tanto, se tipa como `any` para no romper el build.
 */
// eslint-disable-next-line @typescript-eslint/no-explicit-any
export type Database = any;
